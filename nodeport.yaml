apiVersion: v1
kind: Service
metadata:
  labels:
    app: myapp01
  name: myapp01-np
spec:
  ports:
  - port: 8000
    protocol: TCP
    targetPort: 80
  selector:
    app: myapp01
  type: nodeport



apiVersion: v1
kind: Service
metadata:
  labels:
    app: myapp01
  name: myapp01-np
spec:
  ports:
  - port: 8000
    protocol: TCP
    targetPort: 80
  selector:
    app: myapp01
  type: nodeport
